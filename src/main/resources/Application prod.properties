spring.application.name=backend-wekily

server.port=8080

# ─── Database — pointe vers le service "db" du docker-compose ─
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/infosoft_transd?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
spring.datasource.username=${DB_USER:infosoft_10872}
spring.datasource.password=${DB_PASSWORD:Mok065681325}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ─── JPA ──────────────────────────────────────────────────────
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.hbm2ddl.auto=update

# ─── Logs ─────────────────────────────────────────────────────
logging.level.root=WARN
logging.level.com.example.backendwekily=INFO

SPRING_PROFILES_ACTIVE=prod